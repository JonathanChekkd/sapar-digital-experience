import { AppExperience } from "@/components/app-experience";
import type { ReactElement } from "react";

export default function CreatePage(): ReactElement {
  return <AppExperience view="create" />;
}
